#include<stdio.h>
int main()
{
float length,width,area;
printf("Enter the value for length:");
scanf("%f",&length);
printf("Value for length is = %f\n",length);
printf("Enter the value for width:");
scanf("%f",&width);
printf("Value for width is = %f\n",width);
area = length*width;
printf(" Area of rectangle is = %f\n",area);
}
