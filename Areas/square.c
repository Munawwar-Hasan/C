#include<stdio.h>
int main()
{
float side,area;
printf("Enter the value of side:");
scanf("%f",&side);
printf("Value of side is = %f\n",side);
area = side*side;
printf("Area of square is = %f\n",area);
}

